#项目启动端口号
server.port=8089

#=================数据库相关配置==========================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1/online_myclass?serverTimezone=GMT%2B8&userUnicode=true&characterEncoding=utf-8&useSSL=false
spring.datasource.username=root
spring.datasource.password=******

#开启控制台打印sql语句
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

#Mybatis 下划线转驼峰配置
mybatis.configuration.map-underscore-to-camel-case=true

#配置扫描
mybatis.mapper-locations=classpath:mapper/*.xml

#配置xml的结果别名
mybatis.type-aliases-package=com.example.demo.domain.entity

#配置spring-security用户名,密码
spring.security.user.name=root
spring.security.user.password=******
